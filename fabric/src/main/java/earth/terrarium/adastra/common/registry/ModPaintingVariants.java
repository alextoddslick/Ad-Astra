package earth.terrarium.adastra.common.registry;

// In 1.21.1, PaintingVariant is data-driven (defined in data packs, not code).
// Painting variants should be defined in: data/ad_astra/painting_variant/*.json
// TODO: Create data pack JSON files for these painting variants
@SuppressWarnings("unused")
public class ModPaintingVariants {
    // Painting variants are now data-driven in 1.21.1
    // Previously registered: mercury, moon, pluto, earth, glacio, mars, venus,
    // jupiter, neptune, uranus, saturn, the_milky_way, alpha_centauri, sun

    public static void init() {
        // No-op: paintings are data-driven in 1.21.1
    }
}
